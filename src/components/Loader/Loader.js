import {BallTriangle} from "react-loader-spinner";

const Loader = () => {
  return (
    <section>
      <BallTriangle
        color="#00BFFF"
        height={300}
        width={300}
      />
    </section>
  )
};

export default Loader;
